//Write yore solution in this file 
function WriteCards(names, happy, ="happy"){
    let card =[];
    for (let r = 0; r < names.lenghth; r++){
        card.push(`Thank  you,${names[r]},for the wonderful ${happy}gift!`);
    }
console.log(card);
return card;
}
function countDown(number){
    let h=0
    while(number >=h){
        console.log(number);
        number;
    }
}
